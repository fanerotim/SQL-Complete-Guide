-- START TRANSACTION; -- MySQL
-- ROLLBACK;

-- DELETE FROM customers
-- WHERE first_name = 'Genoveva'

-- INSERT INTO customers (
--     first_name,
--     last_name,
--     email
-- )
-- VALUES(
--     'Genoveva',
--     'Petkova',
--     'genoveva@gmail.com'
-- );

-- INSERT INTO orders(
--     amount_billed,
--     customer_id
-- )
-- VALUES(
--     52.2
-- );

