-- Find the table with max seats
-- SELECT MAX(seats) FROM tables;

-- Find the table with min seats
-- SELECT MIN(seats) FROM tables;

-- Find the max amount_billed and max amount_tipped
-- SELECT MAX(amount_billed), MAX(amount_tipped) FROM bookings;

